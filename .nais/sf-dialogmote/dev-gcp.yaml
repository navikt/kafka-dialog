apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: {{ DEPLOY_APP }}
  namespace: team-dialog
  labels:
    team: team-dialog
spec:
  webproxy: true
  image: {{ image }}
  port: 8080
  replicas:
    min: 1
    max: 1
  liveness:
    path: /isAlive
    initialDelay: 5
    periodSeconds: 5
    failureThreshold: 30
  readiness:
    path: /isReady
    initialDelay: 5
    periodSeconds: 5
    failureThreshold: 30
  prometheus:
    enabled: true
    path: /metrics
  resources:
    limits:
      cpu: 1000m
      memory: 4096Mi
    requests:
      cpu: 100m
      memory: 2048Mi
  accessPolicy:
    outbound:
      external:
        - host: test.salesforce.com
        - host: navdialog--preprod.my.salesforce.com
        - host: navdialog--uat.my.salesforce.com
  envFrom:
    - secret: sf-dialogmote
  filesFrom:
    - secret: sf-dialogmote
  kafka:
    pool: nav-dev # enum of nav-dev, nav-prod
  env:
    - name: POSTER_SETTINGS
      value: ENCODE_KEY, AVRO_VALUE, RUN_ONCE, SAMPLE, FROM_BEGINNING
    - name: KAFKA_CLIENTID
      value: sf-dialogmote-v1
    - name: KAFKA_SECURITY
      value: 'TRUE'
    - name: KAFKA_SECPROT
      value: SASL_SSL
    - name: KAFKA_SASLMEC
      value: PLAIN
    - name: KAFKA_TOPIC
      value: teamsykefravr.isdialogmote-dialogmote-statusendring
    - name: MS_BETWEEN_WORK
      value: '600000'
    - name: SF_INSTANCE
      value: PREPROD
    - name: SF_TOKENHOST
      value: 'https://test.salesforce.com'
    - name: SF_VERSION
      value: 'v48.0'
    - name: VAULT_INSTANCE
      value: GCP
    - name: DEPLOY_APP
      value: {{ DEPLOY_APP }}