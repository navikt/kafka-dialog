name: Configure and run workflow

on: [push, pull_request]

jobs:
  build-deploy:
    uses: ./.github/workflows/workflow.yml
    secrets: inherit
    with:
      DEPLOY_APP: sf-dialogmote
      DEPLOY_CLUSTER: prod-gcp